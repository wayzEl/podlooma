name = "podlooma-tts"
pages_build_output_dir = "./"

# No kv_namespaces here - they should be configured via dashboard for Pages
# Functions will be automatically detected from the functions/ directory

[build]
command = ""

# For Pages Functions, KV bindings are configured in the Cloudflare dashboard
# under Settings → Functions → KV namespace bindings 